/* eslint-disable import/no-anonymous-default-export */

const initialState = {
  username: '',
  email: '',
  password: ''
}

export default (state=initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}